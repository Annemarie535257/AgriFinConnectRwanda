# Copy to .env.local for local dev (optional). For Netlify, set these in Site settings → Environment variables.

# Backend API base URL (no trailing slash). Use your Render URL when frontend is on Netlify.
VITE_API_URL=https://agrifinconnectrwanda.onrender.com/api

# Backend base URL for links (e.g. Django admin). Derive from VITE_API_URL if not set.
VITE_BACKEND_URL=https://agrifinconnectrwanda.onrender.com
