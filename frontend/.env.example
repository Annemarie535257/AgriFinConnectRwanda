# Optional overrides. By default, frontend uses same-origin:
# - API calls -> /api
# - Backend links -> current site origin
# Set these only when frontend and backend are on different hosts.

# VITE_API_URL=https://api.example.com/api
# VITE_BACKEND_URL=https://api.example.com
