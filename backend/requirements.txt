# AgriFinConnect Rwanda backend
#
# Export chatbot (tf_model.h5 -> pytorch_model.bin) for Render:
#   py -3.11 -m venv venv_export
#   .\venv_export\Scripts\Activate.ps1
#   pip install -r backend/requirements.txt   # wait for it to finish (no other pip in venv_export)
#   python backend/export_chatbot_to_pytorch.py
# Python 3.9-3.12 required so tensorflow and torch install.
#
# Django & API
Django>=4.2,<5.1
djangorestframework>=3.14
django-cors-headers>=4.3
drf-yasg>=1.21

# ML core
joblib>=1.3
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Chatbot & translation (PyTorch + transformers used on Render; TF only needed for
# the local export_chatbot_to_pytorch.py script — see requirements-export.txt).
# On Render, set CHATBOT_DISABLED=1 env var if the instance runs out of memory.
transformers>=4.30,<5.0
sentencepiece>=0.1.99
huggingface-hub>=0.20
# tensorflow and tf-keras are intentionally omitted here — they are only needed for
# the local export script. Use requirements-export.txt for that instead.
torch>=2.2; python_version >= "3.9" and python_version < "3.13"

# Server & DB
gunicorn>=21.0
psycopg2-binary>=2.9
dj-database-url>=2.1
whitenoise>=6.6
Pillow>=10.0
