# AgriFinConnect Rwanda backend
#
# Export chatbot (tf_model.h5 -> pytorch_model.bin) for Render:
#   py -3.11 -m venv venv_export
#   .\venv_export\Scripts\Activate.ps1
#   pip install -r backend/requirements.txt   # wait for it to finish (no other pip in venv_export)
#   python backend/export_chatbot_to_pytorch.py
# Python 3.9-3.12 required so tensorflow and torch install.
#
# Django & API
Django>=4.2,<5.1
djangorestframework>=3.14
django-cors-headers>=4.3
drf-yasg>=1.21

# ML core
joblib>=1.3
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Chatbot & translation (backend uses PyTorch first; TF needed for export_chatbot_to_pytorch.py)
# Use Python 3.9–3.12 for this venv so tensorflow and torch install (e.g. py -3.11 -m venv venv_export).
transformers>=4.30,<5.0
tensorflow>=2.13; python_version >= "3.9" and python_version < "3.13"
tf-keras>=2.17.0; python_version >= "3.9" and python_version < "3.13"
sentencepiece>=0.1.99
huggingface-hub>=0.20
torch>=2.2; python_version >= "3.9" and python_version < "3.13"

# Server & DB
gunicorn>=21.0
psycopg2-binary>=2.9
dj-database-url>=2.1
whitenoise>=6.6
Pillow>=10.0
