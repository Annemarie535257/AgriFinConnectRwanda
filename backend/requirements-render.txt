# AgriFinConnect Rwanda — Render production requirements
# Excludes torch/transformers (chatbot disabled via CHATBOT_DISABLED=1 on Render).
# To enable the chatbot on Render, add torch and transformers back and set
# CHATBOT_MODEL_HF_REPO to your Hugging Face repo name instead.
#
# Generated from backend/requirements.txt with AI/ML inference libs removed
# because Render free tier (512 MB RAM) cannot hold PyTorch in memory.

# ── Django & REST API ──
Django>=4.2,<5.1
djangorestframework>=3.14
django-cors-headers>=4.3
drf-yasg>=1.21

# ── ML core (loan default risk + fraud detection with scikit-learn) ──
joblib>=1.3
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# ── Server & DB ──
gunicorn>=21.0
psycopg2-binary>=2.9
dj-database-url>=2.1
whitenoise>=6.6
Pillow>=10.0
pdfplumber>=0.10
twilio>=9.0

# ── HuggingFace hub (lightweight, used for model downloads if chatbot is enabled) ──
huggingface-hub>=0.20
sentencepiece>=0.1.99
